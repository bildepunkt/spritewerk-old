define([
    './config',
    './canvas',
    '../lib/protos'
], function(config, canvas, Protos) {

    /**
     *
     */
    return Protos.extend({
        protosName: 'state',

        setup: function(data) {
            
        }
    });
});